description = 'This package contains the basic functionalities that all Mobile Center services use to communicate with the backend.'
evaluationDependsOn(':sdk')


dependencies {

    compile "com.android.support:support-fragment:${rootProject.ext.supportLibVersion}"
}